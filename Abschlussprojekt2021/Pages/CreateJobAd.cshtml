@page
@model CreateJobAdModel
@inject SyncfusionOptionsService SyncfusionOptions

@{
    ViewData["Title"] = "Create Job Ad";
}

<div class="row">
    <div class="col-md-12 px-0">
        <form method="post">
            <div class="form-group col-md-4">
                <label asp-for="JobAdDto.Name"></label>
                <input asp-for="JobAdDto.Name" autocomplete="off" class="form-control" placeholder=@Constants.formLastVornameNachname />
                <span class="text-danger" asp-validation-for="JobAdDto.Name"></span>
            </div>
            <div class="form-group col-md-4">
                <label asp-for="JobAdDto.Position"></label>
                <input asp-for="JobAdDto.Position" autocomplete="off" class="form-control" placeholder=@Constants.formJobAdName />
                <span class="text-danger" asp-validation-for="JobAdDto.Position"></span>
            </div>
            @*<div class="form-group col-md-4">
        <label asp-for="Input.Description"></label>
        <input asp-for="Input.Description" autocomplete="off" class="form-control" />
        </div>*@
            <div class="form-group col-md-8">
                <label asp-for="JobAdDto.Description"></label>
                <!-- RTE Component -->
                <ejs-richtexteditor id="rteCreate"
                                    editorMode="HTML"
                                    enableTabKey="true"
                                    ejs-for="JobAdDto.Description"
                                    placeholder=@Constants.rtePlaceholder
                                    showCharCount="true"
                                    maxLength="2000"
                                    beforeSanitizeHtml="beforeSanitizeHtml"
                                    enableResize="true">
                    <!-- Cleanup copied content from the web -->
                    <e-richtexteditor-pastecleanupsettings plainText="true" />
                    <!-- Toolbar setting - The lists with the RTE options are no longer in the code behind, but can now be injected directly into the view. -->
                    <e-richtexteditor-toolbarsettings items=@SyncfusionOptions.GetRTEOptionsListMinimal();
                                                      enable="true">
                    </e-richtexteditor-toolbarsettings>
                    <e-content-template>

                    </e-content-template>
                </ejs-richtexteditor>
                <!-- RTE Component -->
            </div>
            <div class="form-group col-md-4">
                <label asp-for="JobAdDto.MainSkills"></label>
                <input asp-for="JobAdDto.MainSkills" autocomplete="off" class="form-control" placeholder=@Constants.formMainSkills />
            </div>
            <div class="form-group col-md-4">
                <label asp-for="JobAdDto.Region"></label>
                <input asp-for="JobAdDto.Region" autocomplete="off" class="form-control" placeholder=@Constants.formRegion />
            </div>
            <div class="form-group col-md-4">
                <label asp-for="JobAdDto.StartDate"></label>
                <input asp-for="JobAdDto.StartDate" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
            </div>

            <!-- Buttons -->
            <div class="col-md-4">
                <button type="submit" class="btn btn-secondary">@Constants.BtnSave</button>
                <button type="submit" class="btn btn-secondary" asp-page-handler="CancelButton" formnovalidate>@Constants.BtnCancel</button>
            </div>
        </form>
    </div>
</div>

<!-- Data Annotations (code behind) -->
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<style>
/* Syncfusion RTE - Min and Max Resize Value */
.e-richtexteditor {
    min-width: 500px;
    max-width: 800px;
    min-height: 200px;
    max-height: 350px;
}
/* -------------------------------------------------- */
</style>